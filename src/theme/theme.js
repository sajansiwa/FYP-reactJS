import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#3D5A80",
    },
    secondary: {
      main: "#E0FBFC",
    },
    success: {
      main: "#4caf50",
    },
  },
});

export default theme;
